apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: us-cluster-ready
spec:
  description: "Phase 1 — Verify us spoke cluster is operational with 2 nodes"
  steps:
  - try:
    - assert:
        resource:
          apiVersion: v1
          kind: Node
        count: 2
    cluster: us
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: chainsaw-cronjob-running
spec:
  description: "Phase 7 — Verify Chainsaw CronJob is deployed"
  steps:
  - try:
    - assert:
        resource:
          apiVersion: batch/v1
          kind: CronJob
          metadata:
            namespace: monitoring
            name: chainsaw-runner
        timeout: 30s
    cluster: hub